/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'res.cloudinary.com', 'placehold.it', 'cdn-icons-png.flaticon.com'],
    }
}

module.exports = nextConfig
